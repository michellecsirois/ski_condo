<h2>Users</h2>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %>: <%= user.email %>
    </li>
  <% end %>

</ul>

<%= link_to "New User", new_user_path %>
<br>
<%= link_to "Home", root_path %>
